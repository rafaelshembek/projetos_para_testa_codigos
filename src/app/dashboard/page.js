import React, { } from "react";

export default function DashBoard() {
    return (
        <main className="bg-white w-full text-zinc-600">
            <h1>Pagina dashboard....</h1>
        </main>
    )
}